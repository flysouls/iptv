{
  "name": "iptv",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build": "node index.js",
    "merge": "node merge.js",
    "fetch": "node fetch.js",
    "clear": "node clear.js",
    "start": "npm run clear && npm run fetch && npm run build && npm run merge",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.6.8",
    "node-fetch": "^3.3.2",
    "progress": "^2.0.3"
  },
  "devDependencies": {
    "@types/node": "^22.5.4"
  }
}
